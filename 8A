#include <bits/stdc++.h>

using namespace std;

int main()
{
    double a,b,c,x,y;
    cin >> x >> y >>  a >> b >> c;
    if(abs(a*x+b*y+c)<1e-6){
        cout << "YES";
        return 0;
    }
    cout << "NO";
}
