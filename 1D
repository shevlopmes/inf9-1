#include <iostream>
#include<math.h>
using namespace std;

int main()
{
    int a,b,c,d,e;
    cin >> a >> b >> c >> d >> e;
    int f = 0;
    for(int x = 0; x <= 1000; x++){
        if(a*x*x*x+b*x*x+c*x+d == 0 && x != e){
            f++;
        }
        
    }
    cout << f;
}
