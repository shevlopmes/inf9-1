#include <iostream>
#include<math.h>
using namespace std;
int sign(int a){
    if(a>0) return 1;
    if(a<0) return -1;
}
int main()
{
    int x,y;
    cin >> x;
    while(cin >> y){
        if(sign(y)==sign(x)){
            cout << x << " " << y;
            return 0;
        }
        x = y;
    }

}
